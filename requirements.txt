# Lockfile normativo M1/M2 (piano M2 §3/§7.8): il fingerprint di
# determinismo è una regressione stesso-ambiente documentata (QA-F11).
numpy==2.4.2
polars==1.43.2
PyYAML==6.0.3
pytest==9.1.1
# Ambiente verificato M1/M2 (2026-08-10): Python 3.14.6
# Lockfile ML (D-ML-1 ratificato 2026-08-11): logistic multinomiale deterministica (lbfgs)
scikit-learn==1.9.0
scipy==1.18.0
# M6 (ADR-0016, pin 2026-08-12): server+client OPC UA embedded (fork asyncua)
asyncua==2.0.1
# M8 (ADR-0019): consumer MQTT (pipeline/ingest.py) — pin verificato 2026-08-12 (pip index versions: ultima 2.x = 2.1.0)
paho-mqtt==2.1.0
# M10 (ADR-0021): storage operazionale PostgreSQL + API (spec .scratch/m10/spec.md §8)
# Pin verificato 2026-08-13 (pip show sqlalchemy psycopg fastapi uvicorn httpx pytest)
# — lockfile normativo: nessun intervallo >=.
sqlalchemy==2.0.52
psycopg[binary]==3.3.4
fastapi==0.140.0
uvicorn[standard]==0.51.0
# (test-only: httpx per fastapi TestClient — NON richiesto a runtime dell'API)
# Presente nel lockfile così che pipeline/tests/test_api.py raccolga in un env
# fresco (FINDING M10-H1: senza httpx il modulo fallisce alla collection).
httpx==0.28.1
